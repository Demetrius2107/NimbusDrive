# 开发路线图 v1

> 本文档追踪 NimbusDrive 的分支级开发计划与进展，便于团队回溯。
> **新计划一律追加到本文档**（而非散落在对话或临时文件中），完成后在对应条目标注状态与合入提交。

---

## 一、已完成

### feat/store-and-auth ✅
- **状态**：已合入 master（PR #1）
- **范围**：sqlx + pgx 数据层、UserRepo/FileRepo/FileHashRepo/UploadSessionRepo/ShareRepo、JWT 鉴权（register/login/me）、Gin APIServer 骨架、Hertz TransferServer 骨架、前端 React + Antd + Zustand 骨架

### chore/license-and-cleanup ✅
- **状态**：已合入 master（PR #3）
- **范围**：补 Apache 2.0 LICENSE；从 git 历史中清除 .atomcode/ 目录

### feat/upload ✅
- **状态**：已推送 feat/upload，待开 PR 合入 master
- **范围**：TransferServer 上传完整链路
  - migration 0002：`upload_sessions.upload_id` 列
  - storage/minio.go：基于 `minio.Core` 的分块上传 API
  - middleware/hertz_auth.go：Hertz JWT 鉴权
  - handler/upload_handler.go：check-hash（秒传判定+创建会话）、PUT chunk、GET missing（断点续传）、POST complete（合并+元数据事务）、DELETE cancel
  - cmd/transferserver：注入依赖 + 挂载 /upload 路由
  - web/uploader.ts：mock 替换为真实接口；SHA-256 哈希；vite 代理分流 :8081/:8080
- **设计决策**：
  - check-hash 放 TransferServer（非设计文档 7.1 的 APIServer），减少跨服务调用
  - complete 仅校验 size，不做服务端全量 SHA-256
  - 配额扣减用原子条件 UPDATE `WHERE used+delta <= quota`，事务内执行防超卖
- **待办**：完整 e2e 链路验证（需本地 PG + MinIO）

### feat/persistence-gc-outbox ✅
- **状态**：已完成，待推送远程开 PR
- **服务**：APIServer（控制面，crons 全跑此侧）
- **范围**：
  - 持久层审计 + repo `sqlx.ExtContext` 接口化（Upsert/DecrRef/ListZeroRef/MarkCompleted/IncrUsedStorage 接受 DB 或 Tx，传播事务，消除内联 SQL 漂移，修 `MarkCompleted` size 自赋值 bug）
  - migration 0003/0004/0005：`quota_periods` DDL 纳入 SQL 迁移、`file_hashes.zero_ref_at` 墓碑列 + 部分索引、`outbox` 表 + 未投递索引
  - `DecrRef` 墓碑化（不再立即 DELETE 零引用行）+ `Upsert` 清 `zero_ref_at`
  - `internal/cron/`：TrashPurge（递归 CTE 子树 + 批量 DecrRef + HardDeleteRecursive + 配额回补）、HashGC（墓碑 + 24h grace + MinIO RemoveObject + DeleteIfZeroRef 再校验）、SessionExpiry（MarkExpired + 占位文件 HardDelete + AbortMultipartUpload）、OutboxRelay（`FOR UPDATE SKIP LOCKED` + XAdd + 回写 published_at）
  - `internal/store/outbox_repo.go`：OutboxRepo（Enqueue/FetchPending/MarkPublished/IncAttempt/CountPending）+ `NewOutboxRepo` 构造器
  - outbox 写入点接线：`completeTransaction` / `instantUpload` 同事务 Enqueue `file.uploaded`；outbox 为 nil 时降级 `emitter.Emit`
  - `eventbus.Emitter` 加 `Publish`（同步 XAdd，relay 用）
  - `config.Cron` 段 + `config.dev.yaml`；metrics 新仪器 7 个（HashGC reclaimed/failed、TrashPurge deleted、SessionExpiry swept、OutboxRelay published/errors、OutboxPending gauge）
  - APIServer main.go 装配 4 cron（PG+MinIO→3 清理 cron，PG+emitter→relay）+ TransferServer main.go 传 outbox
  - 单测：repo executor 接口、DecrRef tombstone、GC 删除序竞态模拟（fake MinIO）、outbox relay 往返、trash 批次
- **深挖点**：分布式 GC 的可达性设计（tombstone + grace + 安全删除序的竞态分析，非"写个 cron"）；事务型 outbox 把"恰好一次"降级为"至少一次 + 幂等"的工程权衡；`FOR UPDATE SKIP LOCKED` 多实例无主并发；`sqlx.ExtContext` executor 接口模式（Go DAL 不靠 DI 框架传播事务的惯用法）
- **依赖**：无新外部依赖（cron 用标准库 `time.Ticker`）
- **前置**：feat/async-events ✅（事件总线 + 消费端 SETNX 幂等）、feat/observability-metrics ✅（仪器接入）
- **文档**：`docs/protocol-specs/persistence-gc.md`、设计决策 #10

---

## 二、待开发（按建议优先级排序）

### feat/webdav（P2 协议第一批 · 一）🔄 进行中
- **状态**：分支 feat/webdav，设计文档已落 `docs/protocol-specs/webdav.md`
- **服务**：TransferServer（挂载点）+ APIServer（应用密码管理 API）
- **范围**（分期 W1 只读挂载 → W2 可写 → W3 收尾）：
  - 认证：新增应用专用密码（`app_passwords` 表，迁移 0006，bcrypt，可吊销），WebDAV Basic Auth 仅接受应用密码、拒绝主密码；管理端点 POST/GET/DELETE `/api/v1/auth/app-passwords`
  - 挂载：TransferServer 新增 `/dav` 路由组，`golang.org/x/net/webdav.Handler` 经 `common/adaptor` 桥接，实现 `webdav.FileSystem` 适配器桥接到 FileRepo / MinIO / QuotaRepo
  - 路径解析：URL 深路径逐段解析（邻接表无 path 列，不改 schema），Redis 缓存留后续
  - PUT：临时对象流式 multipart + TeeReader 算 sha256 + CopyObject 到内容寻址 key + 事务落 files/file_hashes（秒传免费获得）；覆盖旧文件软删进回收站；配额预检 507
  - DELETE 软删复用回收站；MOVE 映射 Rename/Move；LOCK/UNLOCK 借 MemFS；ETag = sha256
  - nginx 新增 `location /dav/`（proxy_request_buffering off、client_max_body_size 4g）
- **关键决策**：D1 挂 TransferServer 非 Gin/第三进程；D2 应用密码模型（坚果云/GitHub token 同款）；D3 逐段解析不改表；D5 WebDAV 不提供硬删
- **依赖**：无新外部依赖（官方库 golang.org/x/net/webdav）
- **前置**：feat/upload、feat/file-list、feat/download 均已合入 ✅

### feat/docker-deploy（P2）🔄 进行中
- **状态**：分支 feat/docker-deploy，编码完成；镜像构建因本机外网受限（Docker Hub/Go 模块/npm 源）待切换环境后验证
- **服务**：部署（APIServer + TransferServer + 前端 + 中间件）
- **范围**：
  - 根 `Dockerfile`：Go 双服务共用模板（SERVICE 构建参数二选一），golang:1.25-alpine 多阶段构建 → alpine 运行层（ca-certificates + tzdata + 非 root 用户），CGO_ENABLED=0
  - `web/Dockerfile`：node:22 构建 Vite → nginx:1.27 托管 SPA + API 反向代理
  - `web/nginx.conf`：路由与 vite dev proxy 对齐（/api/v1/upload|download → TransferServer；/api/v1/s/download/ 分享兑换 → TransferServer；/api/*（SSE 已关缓冲）→ APIServer；其余 SPA fallback）
  - `configs/config.prod.yaml`：容器拓扑（服务间容器 DNS、log_dir 空=纯 stdout、OTLP→Tempo、public_endpoint=localhost:9000）
  - `deploy/docker-compose.yml`：全栈编排，`include` 复用 infra 栈（健康检查 + 迁移 init 原样保留）；可观测栈放 `--profile observability`（Prometheus 用 `prometheus.docker.yml` 容器名抓取变体）
  - 应用镜像内配置名经 `NIMBUS_CONFIG_NAME` 环境变量切换（两个 main 入口小改）；凭据全链路 `NIMBUS_*` 环境变量覆盖 + `deploy/.env.example`
  - `deploy/README.md`：用法 + 网络受限环境镜像代理说明（lockfile resolved 固定腾讯源坑）
- **本地已验证**：compose config（含 profile）通过、config.prod 字段映射正确、全量单元测试通过、集成测试编译通过
- **深挖点**：镜像分层（builder/runner 分离 + 构建 ARG 注入镜像源，通用 Dockerfile 与本地网络解耦）；nginx 反代路由规则与开发代理对齐（SSE 关缓冲、长读超时）；凭据注入链（.env → compose 变量 → NIMBUS_* → viper）
- **依赖**：无新依赖
- **前置**：无（master 上 deploy/infra、observability 栈已就绪）

### test/integration（P2）🔄 进行中
- **状态**：store 层集成测试编码完成（编译通过），待容器环境跑通
- **范围**：
  - `internal/store/integration_test.go`（`//go:build integration` 标签，`NIMBUS_TEST_DSN` 未设置自动跳过）：6 组用例
  - 哈希引用全生命周期（Upsert 累加 → DecrRef 墓碑化归零不删行 → grace 窗口 ListZeroRef 语义 → re-Upsert 清墓碑移出 GC 候选 → DeleteIfZeroRef 再校验）
  - executor 接口事务传播（Tx 内 Upsert 回滚不留痕）
  - 上传会话位图真实 DB 语义（乱序/重复置位幂等、位图扩容、MissingChunks 补集、Complete 状态机）
  - 存储配额原子条件更新（累计/超限/回补/不为负）
  - 月度传输配额（GetOrCreateCurrent 幂等、派生配额、IncrUpload/IncrDownload、quota=0 不限）
  - outbox 往返（Enqueue → 事务内 FetchPending（FOR UPDATE）→ IncAttempt → MarkPublished 不再出现）
- **待补**：handler 层 e2e（依托全栈 compose 拉起完整系统走 HTTP 流程）
- **依赖**：deploy/infra compose（或全栈）的 PG；`go test -tags integration ./internal/store/`
- **前置**：无

### feat/download（P1）✅
- **状态**：已合入 master
- **服务**：TransferServer (Hertz)
- **范围**：
  - `GET /api/v1/download/:fileId`：鉴权 + 软删除/权限校验 → MinIO GetObject(range) 流式回写
  - `HEAD /api/v1/download/:fileId`：预检文件大小与类型
  - 标准 HTTP Range 头，支持 206 Partial Content（bytes=start-end / bytes=0- / bytes=-99）
  - Content-Disposition RFC 5987 编码处理中文文件名
  - 前端 downloader.ts + FilesPage 下载操作列
  - 可选 P2 优化：预签名 URL 让客户端直连 MinIO
- **依赖**：files 表读取（FileRepo.GetByID）、storage.MinIO.GetObject
- **前置**：feat/upload 合入 ✅

### feat/file-list（P1）✅
- **服务**：APIServer (Gin)
- **范围**：
  - 文件列表：`GET /files`（分页、按 parent_id）
  - 创建文件夹：`POST /files/folder`
  - 移动：`POST /files/:id/move`
  - 重命名：`POST /files/:id/rename`
  - 软删除：`DELETE /files/:id`（文件夹递归软删除）
  - 回收站列表：`GET /trash`
  - 恢复：`POST /trash/:id/restore`
  - 彻底删除：`DELETE /trash/:id`（文件夹递归硬删除 + ref_count--，触发 GC 候选）
- **Store 补充**：ListTrash、SoftDeleteRecursive、HardDeleteRecursive、ListDescendants（递归 CTE）
- **前端**：FilesPage 后端加载 + 文件夹导航 + 面包屑 + 新建/重命名/移动/删除；新增 TrashPage + 侧栏入口
- **依赖**：FileRepo 已有的 ListByParent/Move/Rename/SoftDelete/Restore/Subtree/HardDelete
- **前置**：无
- **状态**：已本地合入 master，待推送远程开 PR

### feat/share（P2）✅
- **服务**：APIServer (Gin) + Redis 缓存
- **范围**：
  - 创建分享：`POST /files/:id/share`（生成 16 字符短 ID、bcrypt 密码、过期、次数限制）
  - 我的分享列表：`GET /shares`
  - 取消分享：`DELETE /shares/:id`（清缓存）
  - 公开查看：`GET /s/:id`（不暴露密码，has_password）
  - 校验密码：`POST /s/:id/validate`（bcrypt 校验 + access_count++ + 过期判断）
  - Redis write-through 缓存：`share:{id}` Hash + TTL
- **Store 补充**：MarkExpired、GetByFile
- **Cache 补充**：SetShare/GetShare/DelShare/IncrShareAccess
- **前端**：SharesPage 重写（列表/取消/复制链接）、ShareViewPage 公开访问页（/s/:id）、FilesPage 分享入口
- **依赖**：ShareRepo（Create/Get/IncrAccess/Cancel/ListByUser + 新增 MarkExpired/GetByFile）
- **状态**：已完成，待推送远程开 PR

### test/upload（P2）✅
- **服务**：测试
- **范围**：
  - store 层纯函数单测：位图工具 ensureBitmapLen/setBit/getBit（扩容/保留/越界/高位在前/断点续传场景）
  - handler 层纯函数单测：detectMIME（扩展名映射/大小写）、parseRange（三种格式/截断/多段降级/非法）、buildContentDisposition（ASCII/RFC5987）、isASCII、parseFileID 逻辑
- **未覆盖（需 PG + MinIO 集成环境，留后续）**：
  - store 层 DB 集成：UploadSessionRepo.MarkChunkUploaded、FileHashRepo.Upsert/DecrRef、UserRepo.IncrUsedStorage
  - handler 层 e2e：check-hash 秒传/未命中、complete 事务回滚、cancel 清理
- **前置**：feat/upload 合入后接口已稳定 ✅
- **状态**：纯函数单测已完成，集成测试待 PG+MinIO 环境就绪

### feat/admin（P2）✅
- **服务**：APIServer (Gin) + GORM adminstore
- **范围**：
  - 用户管理：`GET /admin/users`（分页/搜索/状态筛选）、`PATCH /admin/users/:id/status`（封禁/解封）、`PATCH /admin/users/:id/quota`（调配额）
  - 文件审计：`GET /admin/files`（全局文件列表、用户筛选）
  - 操作日志：`GET /admin/logs`（分页、按 action/actor 筛选）
  - **操作日志异步聚合器**（核心深挖点）：channel + 批量 INSERT + 背压（channel 满丢弃+计数）+ 优雅关闭（Close 排空 + wg.Wait）
- **GORM 模型**：User/OperationLog/File + AutoMigrate
- **前端**：AdminPage 三 Tab（用户管理/文件审计/操作日志）+ 侧栏仅 is_admin 可见 + 登录接入真实 API 存储 is_admin
- **依赖**：adminstore (GORM)、operation_logs 表
- **状态**：已完成，待推送远程开 PR

### feat/protocol-specs（P3）
- **范围**：
  - JSON Schema（draft 2020-12）机器可读契约：`share.create`/`share.validate`/`upload.check-hash`
  - 契约加载器（`//go:embed` + 启动期编译 + 运行期 O(1) 查找）
  - Gin/Hertz 契约校验中间件（格式校验下沉，handler 只做语义校验）
  - `docs/protocol-specs/` 协议规范（upload/download/share wire 细节 + gap 清单）
- **状态**：已完成，待推送远程开 PR

### feat/async-events（P3）
- **范围**：
  - Redis Streams 事件总线：Emitter（背压 + 优雅关闭）+ Consumer 框架（消费者组 + 至少一次 + 幂等 + DLQ + 优雅关闭）
  - 4 类事件：`file.uploaded`（含秒传）、`share.created`、`share.accessed`、`user.registered`
  - 4 个消费者：审计日志同步、用户日上传量统计、配额用量对账、no-op 骨架
  - EventBusConfig + 双服务启动接入（APIServer 消费 + TransferServer 只生产）
- **深挖点**：消费者侧工程化（幂等 SETNX、毒丸 DLQ、XAUTOCLAIM 接管超时消息、LIFO 关闭顺序）
- **状态**：已完成，待推送远程开 PR

### feat/presigned-download（P2）
- **服务**：TransferServer (Hertz) + APIServer (Gin) 协作
- **范围**：
  - owner 预签名下载：`GET /download/:fileId/presign`（TransferServer，JWT + owner 校验，签发直连 MinIO URL）
  - 分享下载能力令牌：APIServer `ValidateShare` 校验通过后签发（32 字节 hex，Redis SET TTL 5min）
  - 分享下载兑换：`GET /s/download/:token`（TransferServer，公开，Lua 原子 GET+DEL 单次消费 → 查 files → 签预签名 URL）
  - MinIO 公网端点分离（`public_endpoint`）：内部上传走内网，预签名 URL 用客户端可达端点
  - 响应头覆盖（`response-content-type`/`response-content-disposition`）：浏览器直连以正确文件名保存
- **深挖点**：capability-based security（授权决策与资源访问解耦，令牌即授权凭证）；单次消费防重放（Lua GET+DEL）；预签名 URL 签名覆盖 query 不覆盖 Range（一 URL 多次分块下载）
- **依赖**：files 表（FileRepo.GetByID）、storage.MinIO（PresignClient + PresignedDownloadURL）、cache.Redis（令牌签发/兑换）
- **前置**：feat/download ✅、feat/share ✅、feat/async-events ✅（ValidateShare 已发 share.accessed 事件）
- **状态**：已完成，待推送远程开 PR（合入提交 0608f86）

### feat/quota-streaming（P2）
- **服务**：APIServer (Gin)
- **范围**：
  - `quota_periods` 表 + GORM 模型 + 迁移（月度传输配额，每用户每月一行，period `YYYY-MM`）
  - `store.QuotaRepo`（sqlx）：`GetOrCreateCurrent` / `CheckUpload` / `IncrUpload` / `IncrDownload` / `ResetMonthly` / `ResetMonthlyAll`（原子条件 UPDATE，超限返回 `ErrQuotaExceeded`）
  - 上传配额校验：`CheckHash` 前置检查（只检查不扣）；`file.uploaded` 事件 → `QuotaConsumer` → `IncrUpload` 累计
  - 下载配额校验：预签名签发时 `IncrDownload`（按文件大小计量，分享下载计入文件所有者）
  - 批量配额重置：`POST /admin/users/quota/reset`（广播 `reset_all`）
  - SSE 端点：`GET /quota/stream`（JWT，支持 `?token=` query；版本号 + Last-Event-ID 补发 + 30s 心跳 + 503 降级）
  - `quota.Notifier`：Redis pub/sub 实时扇出 + Stream 持久化补发 + INCR 版本号
  - `quota.MonthlyResetCron`：进程内每 6h 检查月初，幂等批量建当月行
  - `GinJWTAuthAllowQuery`：SSE 专用鉴权中间件（EventSource 无法设自定义头）
  - 前端：`useQuotaStore` + `useQuotaStream`（EventSource + 降级轮询）+ QuotaPage/AppLayout 真实数据
- **深挖点**：推送工程可靠性——连接管理（断线自动重连）、版本号防丢失更新（Last-Event-ID 补发）、跨实例广播（Redis pub/sub 扇出）、降级（SSE 503 → 轮询）；这套模式换 WebSocket/gRPC stream 一条不少
- **依赖**：quota_periods 表、users 表、cache.Redis（pub/sub + Stream + INCR）、eventbus（file.uploaded 事件）
- **前置**：feat/async-events ✅、feat/presigned-download ✅（下载字节计量挂在预签名签发点）
- **状态**：已完成，待推送远程开 PR

### feat/observability-tracing（P2）
- **服务**：APIServer (Gin) + TransferServer (Hertz) 共用
- **范围**：
  - `internal/tracing` 新包：OTel TracerProvider（stdout/otlp/none 可配）+ W3C propagation + traceparent 文本辅助
  - `observability` 配置段（exporter/otlp_endpoint/service_name/sample_ratio）
  - Gin/Hertz trace 中间件：入站提取 traceparent → 起 SpanKindServer span → 出站记录 status
  - logger `FromContext(ctx)`：从 active span 注入 trace_id/span_id 字段
  - 事件总线 trace 传播：`Event.TraceContext` + codec 编解码 + `Emit(ctx, evt)` 注入 + Consumer 提取起 SpanKindConsumer span
  - 分享下载 trace 传播：`ShareDownloadToken.TraceParent`（签发注入/兑换提取）
  - 配额推送 trace 传播：`QuotaChangeEvent.TraceParent` + SSE handler 每条消息起 linked span
  - 双服务 TracerProvider 启动接入 + 中间件挂载（RequestID → Tracer → Logger → Recovery）
- **深挖点**：context carrier 选择、inject/extract 对称性、跨进程边界保持 span 亲子关系——四个边界（HTTP/异步/客户端中介/pub/sub）的 propagation 模式换 Kafka/RabbitMQ/gRPC 一条不少
- **依赖**：go.opentelemetry.io/otel + sdk + stdouttrace + otlptracegrpc
- **前置**：feat/async-events ✅（事件总线 trace 传播）、feat/presigned-download ✅（分享下载 token trace）、feat/quota-streaming ✅（SSE trace）
- **状态**：已完成，待推送远程开 PR

### feat/observability-metrics（P2）
- **服务**：APIServer (Gin) + TransferServer (Hertz) 共用
- **范围**：
  - `internal/metrics` 新包：Collector（RED + 业务仪器）+ Init/Shutdown/Default 全局单例（nil 安全）+ InfraCollector（scrape-time 自定义 Collector）+ MultiConsumer（多 consumer 聚合）
  - `ObservabilityConfig` 扩展 `metrics_enabled`/`metrics_path` + config.dev.yaml
  - Gin/Hertz RED 中间件（route 用 FullPath 模板、status 归并码类、exemplar 从 span context 取 trace_id）
  - 业务埋点：upload（sessions/chunks/completed/bytes/instant-hit）、download（presign/stream/share bytes）、share（created/accessed）、auth（register/login）、quota（monthly reset/admin reset/SSE 活跃连接/SSE events pushed）
  - eventbus infra 埋点：emitter emitted/dropped、consumer pending(XPending)/dlq/processed/errors、logAggregator dropped
  - 双服务 metrics.Init + /metrics 端点（EnableOpenMetrics）+ infra collector 注入
  - `deploy/observability/`：docker-compose（Prometheus/Loki/Tempo/Grafana）+ datasource 自动 provision + 示例 dashboard
- **深挖点**：基数控制（route 模板非原始 URL、status 归并码类、业务 label 刻意低基数）、跨域 histogram bucket（ExponentialBuckets 0.01s..316s 覆盖 API/上传/SSE 三域）、exemplar 关联（ObserveWithExemplar 从 span context 取 trace_id，Grafana P99→trace 直跳）、scrape-time 自定义 Collector（拉模型下派生指标的标准范式，非后台轮询）、三支柱联动（metric exemplar→Tempo trace→Loki 日志）
- **依赖**：prometheus/client_golang v1.24.1（OTel trace 已在 feat/observability-tracing 引入）
- **前置**：feat/observability-tracing ✅（exemplar 从 span context 取 trace_id）
- **状态**：已完成，待推送远程开 PR

---

## 三、计划维护约定

1. **新增计划**：在「二、待开发」追加条目，标注服务、范围、依赖、前置
2. **开工**：将条目状态改为 `🔄 进行中`，注明分支名
3. **完成**：移至「一、已完成」，标注 PR 编号与合入提交哈希
4. **计划变更**：直接在原文档修订，保留历史可在 git log 追溯，不另建副本
