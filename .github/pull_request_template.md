## 变更摘要

<!-- 一句话说明本 PR 做了什么 -->

## 动机

<!-- 为什么做这个改动？关联的 issue / 设计文档章节 -->

## 变更内容

-
-
-

## 测试

- [ ] `GOARCH=amd64 go vet ./...` 通过
- [ ] `GOARCH=amd64 go build ./...` 通过
- [ ] 关键链路手测通过（描述步骤）：
  ```
  1. ...
  2. ...
  ```
- [ ] 前端 `tsc -b` + `vite build` 通过（如涉及）

## 影响范围

<!-- 是否影响现有接口/数据结构/部署？是否有破坏性变更？ -->

## 检查清单

- [ ] 提交信息符合 Conventional Commits（`docs/开发规范.md`）
- [ ] 一个分支只做一件事
- [ ] 未提交敏感信息（密钥/密码/配置）
- [ ] 新增依赖已在 `go.mod` / `package.json` 更新
